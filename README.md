# data-rate-limiter
Token Bucket rate limiter built from scratch in Node.js with Redis as shared state. No libraries, raw algorithm, Express middleware, and a live browser UI to watch tokens drain in real time.
